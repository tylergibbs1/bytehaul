[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "bytehaul"
version = "0.2.0"
description = "Fast file transfer over QUIC"
requires-python = ">=3.8"
license = {text = "Apache-2.0"}

[tool.maturin]
features = ["pyo3/extension-module"]
